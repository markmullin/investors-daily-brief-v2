export const featureFlags = {
    useWebSocket: false,  // Keep this disabled for now
    useRealTimeUpdates: false,  // Keep real-time updates disabled
    useEnhancedAnalysis: true   // Keep enhanced analysis enabled
};